const randomnumber=console.log(parseInt(Math.random()*10+1));
const submit=document.querySelector('#subt');
const userInput=documnet.querySelector('.gueses');
const guessSlot=document.querySelector('.lastResult');
